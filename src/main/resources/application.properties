# Configuração de produção (Heroku/JawsDB) - Ativada para o deploy
spring.datasource.url=jdbc:${JAWSDB_URL}
spring.datasource.username=${JAWSDB_USERNAME}
spring.datasource.password=${JAWSDB_PASSWORD}

# Configuração de desenvolvimento local com banco de dados em memória (H2)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true

# -----
spring.jpa.hibernate.ddl-auto=update
spring.datasource.initialization-mode=always